# To-Do List (Ionic & Angular)

Este é um projeto de um aplicativo simples de lista de tarefas (To-Do List) desenvolvido utilizando o framework Ionic com Angular.

## 🛠️ Tecnologias Utilizadas

* **Ionic Framework**
* **Angular**
* TypeScript
* HTML / SCSS

---

## 📋 Pré-requisitos

Antes de começar, você precisará ter as seguintes ferramentas instaladas em sua máquina:

* **[Node.js](https://nodejs.org/)** (que inclui o **npm**)
* **Ionic CLI** (Interface de Linha de Comando do Ionic)

Para instalar o Ionic CLI globalmente (caso ainda não o tenha), execute o comando abaixo no seu terminal:

```bash
npm install -g @ionic/cli
```

---

## 🚀 Instalação e Configuração

Siga os passos abaixo para configurar o ambiente de desenvolvimento localmente.

1.  **Clone o repositório:**
    ```bash
    # Substitua pela URL do seu repositório
    git clone [https://github.com/seu-usuario/seu-repositorio.git](https://github.com/seu-usuario/seu-repositorio.git)
    ```

2.  **Acesse o diretório do projeto:**
    ```bash
    # Substitua pelo nome do diretório do seu projeto
    cd seu-repositorio
    ```

3.  **Instale as dependências do projeto:**
    Todas as dependências necessárias estão listadas no arquivo `package.json`.
    ```bash
    npm install
    ```

---

## ▶️ Rodando o Projeto

Após a instalação de todas as dependências, execute o seguinte comando para iniciar o servidor de desenvolvimento:

```bash
ionic serve
```

O aplicativo será compilado e aberto automaticamente no seu navegador padrão, geralmente em `http://localhost:8100/`. O servidor monitorará as alterações nos arquivos e recarregará automaticamente o navegador.
